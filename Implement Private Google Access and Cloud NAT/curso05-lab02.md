Crie o script fazendo 
```bash
nano lab.sh
```

Cole o códgio. Aperte option + o, enter para salvar. option + x para sair.

```bash
sudo chmod +x lab.sh
```

para ativar e ./lab.sh para executar o script.